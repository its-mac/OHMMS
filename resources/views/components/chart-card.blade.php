<div class="card">
    <div class="card-header">
        <h5>{{ $title }}</h5>
    </div>

    <div class="card-body">
        <div id="{{ $chartId }}" style="height: {{ $height ?? 320 }}px;"></div>
    </div>
</div>
